package com.projectreddog.ecoshop.item;

public class ItemCredit extends ItemEcoShop {
	/// a placeholder item that all other Currency /credit items can extend so they will all share a common type
	// this also allows them to have the same common set of methods example get value

	public int GetValue() {
		return 0;
	}
}
